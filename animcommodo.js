// Initialize an array called markers
let markers = [];

// Define a variable lookahead with some value
let lookahead = { x: 100, y: 200 };

// Push the lookahead object into the markers array
markers.push(lookahead);

// The markers array now contains one element: the lookahead object
console.log(markers);
