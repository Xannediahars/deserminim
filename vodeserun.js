// Example values for lambda and lambda1
let lambda = 5;
let lambda1 = 3;

// Check if lambda is greater than lambda1
if (lambda > lambda1) {
    lambda1 = lambda; // Assign the value of lambda to lambda1
}

console.log(lambda1); // Output: 5
